# Secrets / env
.env
*.local

# OpenWA session & cache (contient l'auth WhatsApp — NE JAMAIS committer)
_IGNORE_*/
.wwebjs_auth/
.wwebjs_cache/
session*.data.json
*.data.json
openwa-session/
# session OpenWA à la racine uniquement (ne pas attraper zeroclaw/data/)
/data/

# zeroclaw data local (mémoire SQLite, auth profiles)
.zeroclaw/
workspace/

# Données perso (contacts / partenaires) — garder seulement les *.example.json
zeroclaw/data/*.json
!zeroclaw/data/*.example.json

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Logs / pm2
*.log
.pm2/

# OS
.DS_Store
